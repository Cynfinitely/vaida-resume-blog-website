import React from 'react'

const Header = () => {
  return (
    <div className='bg-primaryColor w-screen  text-secondaryColor text-4xl font-semibold'>We are only as strong as the weakest link</div>
  )
}

export default Header